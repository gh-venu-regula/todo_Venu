import classes from './DashboardInfo.module.css'
export default function DashboardInfo() {
    return (
        <div className={classes.dashboardInfo}>
            <p className={classes.plan}>Here’s your plan for the day.</p>
        </div>

    );
}
